name: Multi-Platform Release Build

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      draft:
        description: 'Create Draft Release'
        required: false
        type: boolean
        default: false
      prerelease:
        description: 'Mark as Pre-release'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build (${{ matrix.platform.os }} - ${{ matrix.platform.arch }})
    runs-on: ${{ matrix.platform.runs_on }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: windows
            arch: x64
            runs_on: windows-latest
            target: x86_64-pc-windows-msvc
          - os: linux
            arch: x64
            runs_on: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - os: macos
            arch: aarch64
            runs_on: macos-latest
            target: aarch64-apple-darwin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform.target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux build dependencies
        if: matrix.platform.os == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Backend Tests
        run: cargo test --manifest-path src-tauri/Cargo.toml

      - name: Run Frontend Tests
        run: pnpm test

      - name: Build Tauri Application
        run: pnpm tauri build

      - name: Prepare and Normalize Artifact Names & Checksums (Windows)
        if: matrix.platform.os == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist-artifacts
          $msi = Get-ChildItem -Path src-tauri/target/release/bundle/msi/*.msi | Select-Object -First 1
          if ($msi) {
            $destMsi = "dist-artifacts/the-berry_windows_x64.msi"
            Copy-Item -Path $msi.FullName -Destination $destMsi
            $hash = (Get-FileHash -Path $destMsi -Algorithm SHA256).Hash.ToLower()
            Set-Content -Path "$destMsi.sha256" -Value "$hash  the-berry_windows_x64.msi"
          }
          $exe = Get-ChildItem -Path src-tauri/target/release/bundle/nsis/*.exe | Select-Object -First 1
          if (-not $exe) {
            $exe = Get-ChildItem -Path src-tauri/target/release/*.exe | Where-Object { $_.Name -like "the-berry*" -and $_.Name -notlike "*.d" } | Select-Object -First 1
          }
          if ($exe) {
            $destExe = "dist-artifacts/the-berry_windows_x64.exe"
            Copy-Item -Path $exe.FullName -Destination $destExe
            $hash = (Get-FileHash -Path $destExe -Algorithm SHA256).Hash.ToLower()
            Set-Content -Path "$destExe.sha256" -Value "$hash  the-berry_windows_x64.exe"
          }

      - name: Prepare and Normalize Artifact Names & Checksums (Linux)
        if: matrix.platform.os == 'linux'
        run: |
          mkdir -p dist-artifacts
          appimage=$(find src-tauri/target/release/bundle/appimage -name "*.AppImage" | head -n 1)
          if [ -n "$appimage" ]; then
            cp "$appimage" "dist-artifacts/the-berry_linux_x64.AppImage"
            (cd dist-artifacts && sha256sum the-berry_linux_x64.AppImage > the-berry_linux_x64.AppImage.sha256)
          fi
          deb=$(find src-tauri/target/release/bundle/deb -name "*.deb" | head -n 1)
          if [ -n "$deb" ]; then
            cp "$deb" "dist-artifacts/the-berry_linux_x64.deb"
            (cd dist-artifacts && sha256sum the-berry_linux_x64.deb > the-berry_linux_x64.deb.sha256)
          fi

      - name: Prepare and Normalize Artifact Names & Checksums (macOS)
        if: matrix.platform.os == 'macos'
        run: |
          mkdir -p dist-artifacts
          dmg=$(find src-tauri/target/release/bundle/dmg -name "*.dmg" | head -n 1)
          if [ -n "$dmg" ]; then
            cp "$dmg" "dist-artifacts/the-berry_macos_aarch64.dmg"
            (cd dist-artifacts && shasum -a 256 the-berry_macos_aarch64.dmg > the-berry_macos_aarch64.dmg.sha256)
          fi

      - name: Upload Artifacts to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist-artifacts/*
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || 'latest' }}
          name: ${{ github.ref_type == 'tag' && github.ref_name || 'TheBerry Latest Release' }}
          generate_release_notes: true
          prerelease: ${{ inputs.prerelease || false }}
          draft: ${{ inputs.draft || false }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

